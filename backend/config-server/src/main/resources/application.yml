server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./config
        bootstrap: true

# Config Editor Authentication
# Password: EckertPreisser2025!
# Hash generated with BCrypt (10 rounds)
config:
  auth:
    username: admin
    password: $2a$10$oYsiwL6jJkVRJwSjdfXc5uUSOXfRkGjlJtUWH/LbkLj313HWL8r5a
    jwt:
      secret: eckert-preisser-jwt-secret-super-secure-key-2025-production
      expiration: 86400000  # 24 hours in milliseconds

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,refresh
  endpoint:
    health:
      show-details: always
